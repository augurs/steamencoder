<UserControl x:Class="EncoderApp.Views.StreamDiagnostic"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EncoderApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="400">
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="DiagnosticLabelStyle">
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="DiagnosticValueStyle">
            <Setter Property="Foreground" Value="#e6eef8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
        <Style x:Key="HoverButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="70"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                              Background="{TemplateBinding Background}" 
                              BorderBrush="{TemplateBinding BorderBrush}" 
                              CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2980b9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1f6391"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#777"/>
                                <Setter TargetName="border" Property="Background" Value="#333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#071024" Offset="0"/>
                <GradientStop Color="#06122b" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderBrush="#212a3b" BorderThickness="1" CornerRadius="4" Background="#131d2f" Margin="10">
            <Grid Margin="12,10,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Center" Background="Transparent" Margin="0,0,0,10">
                    <TextBlock Text="Connection Diagnostics" Foreground="#3498db" FontSize="20" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Internet Connection:" Foreground="#94a3b8" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="✓" Foreground="LimeGreen" FontSize="16"/>
                </StackPanel>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="DNS Resolution:" Foreground="#94a3b8" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="✗" Foreground="Red" FontSize="16"/>
                </StackPanel>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Server Pingable:" Foreground="#94a3b8" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="✗" Foreground="Red" FontSize="16"/>
                </StackPanel>
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Server Listening:" Foreground="#94a3b8" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="?" Foreground="Orange" FontSize="16"/>
                </StackPanel>
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Credentials:" Foreground="#94a3b8" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="?" Foreground="Orange" FontSize="16"/>
                </StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="1" BorderBrush="#212a3b" BorderThickness="1" CornerRadius="4" Background="#131d2f" Margin="10,0,10,10" Padding="10">
            <StackPanel>
                <TextBlock Text="Results" Foreground="#3498db" FontSize="20" Margin="0,0,0,10" VerticalAlignment="Center"/>
                <TextBlock Text="• The hostname provided is invalid - it doesn't point to a server." Foreground="#94a3b8" TextWrapping="Wrap" Margin="0,0,0,5"/>
                <TextBlock Text="• The hostname lookup error was: No host name given" Foreground="#94a3b8" TextWrapping="Wrap" Margin="0,0,0,5"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>